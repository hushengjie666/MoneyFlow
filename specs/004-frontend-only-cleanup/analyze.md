# Analysis: 前端项目遗留后端清理

## 关键判断

- 当前运行链路已由 `frontend/src/api-client.js` 的前端实现承载，后端目录可移除。
- 历史测试中存在大量后端耦合用例，需同步删减并替换前端 smoke e2e，保证门禁可持续。

## 风险与控制

- 风险：误删前端仍依赖的测试或模块。  
  控制：删除后执行 `npm run test:all`，并补 smoke e2e 覆盖主入口。
